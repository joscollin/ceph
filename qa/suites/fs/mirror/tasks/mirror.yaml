overrides:
  ceph:
    conf:
      mgr:
        debug client: 10

tasks:
  - cephfs_test_runner:
      modules:
        - tasks.cephfs.test_mirroring.TestMirroring.test_cephfs_mirror_remote_snap_corrupt_fails_synced_snapshot
